[build]
builder = "nixpacks"

[services.backend]
source = "backend"
# This tells Railway to run uvicorn pointing to your 'app' inside 'server.py'
startCommand = "uvicorn server:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/" 

[services.frontend]
source = "frontend"
